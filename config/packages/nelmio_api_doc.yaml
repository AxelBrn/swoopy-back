nelmio_api_doc:
    documentation:
        info:
            title: Swoopy Api
            description: Swoopy is an app to create MCD
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas: # to filter documented areas
        path_patterns:
            - ^/api/v1
